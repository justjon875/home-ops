---
# yaml-language-server: $schema=https://k8s-schemas.m00nlit.dev/cert-manager.io/certificate_v1.json
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: &name postgres-server-ca
spec:
  commonName: postgres-ca
  duration: 8766h # 1 year
  isCA: true
  issuerRef:
    name: cluster-ca
    kind: ClusterIssuer
    group: cert-manager.io
  nameConstraints:
    critical: true
    permitted:
      dnsDomains:
        - .database.svc.cluster.local
        - .database.svc
        - postgres-rw
        - postgres-ro
        - postgres-r
  privateKey:
    algorithm: ECDSA
    size: 384
    encoding: PKCS8
    rotationPolicy: Always
  secretName: *name
  subject:
    countries:
      - SG
    organizations:
      - home-ops
    organizationalUnits:
      - postgres
  usages:
    - cert sign
    - crl sign
---
# yaml-language-server: $schema=https://k8s-schemas.m00nlit.dev/cert-manager.io/issuer_v1.json
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: &name postgres-server-ca
spec:
  ca:
    secretName: *name
