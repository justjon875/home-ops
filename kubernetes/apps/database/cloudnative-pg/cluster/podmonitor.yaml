---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/monitoring.coreos.com/podmonitor_v1.json
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: cluster-18
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: cluster-18
  podMetricsEndpoints:
    - port: metrics
