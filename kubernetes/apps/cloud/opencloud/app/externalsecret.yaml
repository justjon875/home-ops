# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: opencloud
spec:
  secretStoreRef:
    name: bitwarden
    kind: ClusterSecretStore
  target:
    name: opencloud-secret
    template:
      data:
        COLLABORA_ADMIN_USER: admin
        COLLABORA_ADMIN_PASS: "{{ .COLLABORA_ADMIN_PASS }}"
        DECOMPOSEDS3_ACCESS_KEY: "{{ .ACCESS_KEY }}"
        DECOMPOSEDS3_BUCKET: opencloud
        DECOMPOSEDS3_ENDPOINT: https://s3.${ROOT_DOMAIN}
        DECOMPOSEDS3_REGION: us-east-1
        DECOMPOSEDS3_SECRET_KEY: "{{ .SECRET_KEY }} "
  dataFrom:
    - extract:
        key: opencloud
