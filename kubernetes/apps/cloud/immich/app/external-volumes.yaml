apiVersion: v1
kind: PersistentVolume
metadata:
  annotations:
    pv.kubernetes.io/provisioned-by: nfs.csi.k8s.io
  name: immich-nextcloud-pv-nfs
spec:
  capacity:
    storage: 1Ti
  accessModes:
    - ReadOnlyMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: nfs-csi
  mountOptions:
    - nfsvers=4.2
  csi:
    driver: nfs.csi.k8s.io
    readOnly: false
    # volumeHandle format: {nfs-server-address}#{sub-dir-name}#{share-name}
    # make sure this value is unique for every share in the cluster
    volumeHandle: "${NFS_HOST}${NVME_NFS_PATH}#immich#nextcloud#"
    volumeAttributes:
      server: "${NFS_HOST}"
      share: "${NVME_NFS_PATH}/nextcloud"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-nextcloud-pvc
  namespace: dev
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 1Ti
  storageClassName: nfs-csi
  volumeName: immich-nextcloud-pv-nfs

