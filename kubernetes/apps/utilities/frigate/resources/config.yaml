---
mqtt:
  host: mosquitto.utilities.svc.cluster.local
  topic_prefix: frigate
  user: "{FRIGATE_MQTT_USERNAME}"
  password: "{FRIGATE_MQTT_PASSWORD}"

ffmpeg:
  global_args: ["-hide_banner", "-loglevel", "info"]

logger:
  default: debug

database:
  path: /data/frigate.db

detectors:
  coral:
    type: edgetpu
    device: pci

objects:
  track:
    - person

record:
  enabled: true
  retain:
    days: 5
    mode: all
  events:
    retain:
      default: 14
      mode: active_objects

snapshots:
  enabled: true
  timestamp: true
  bounding_box: true
  crop: false

go2rtc:
  streams:
    lounge_hq:
      - rtsp://${FRIGATE_RTSP_USERNAME}:${FRIGATE_RTSP_PASSWORD}@lounge.cams.${ROOT_DOMAIN}:554/stream1
      - "ffmpeg:rtsp_cam#audio=opus"
    lounge_lq:
      - rtsp://${FRIGATE_RTSP_USERNAME}:${FRIGATE_RTSP_PASSWORD}@lounge.cams.${ROOT_DOMAIN}:554/stream2

cameras:
  lounge:
    ffmpeg:
      output_args:
        record: preset-record-generic-audio-aac
      inputs:
        - path: rtsp://127.0.0.1:8554/lounge_hq
          input_args: preset-rtsp-restream
          roles:
            - record
        - path: rtsp://127.0.0.1:8554/lounge_lq
          input_args: preset-rtsp-restream
          roles:
            - detect
            - audio
    detect:
      fps: 15
      width: 640
      height: 480
