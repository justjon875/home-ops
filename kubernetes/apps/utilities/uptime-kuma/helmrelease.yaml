apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: uptime-kuma
  namespace: utilities
spec:
  chart:
    spec:
      chart: app-template
      interval: 15m
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      version: 3.1.0
  install:
    createNamespace: true
    remediation:
      retries: 5
  interval: 15m
  upgrade:
    remediation:
      retries: 5
  values:
    controllers:
      uptime-kuma:
        containers:
          app:
            env:
              TZ: ${TIMEZONE}
            image:
              repository: docker.io/louislam/uptime-kuma
              tag: 1.23.12@sha256:faae8f885a81ff54ab0fecae02e8de73bf7b371bc0a928f9a1e9af3b246d58bd
    ingress:
      app:
        className: nginx
        enabled: true
        hosts:
          - host: &host "uptime.${ROOT_DOMAIN}"
            paths:
              - path: /
                pathType: Prefix
                service:
                  identifier: app
                  port: http
        tls:
          - hosts:
              - *host
    persistence:
      data:
        globalMounts:
          - path: /app/data
        path: ${NFS_PATH}/uptime-kuma
        server: ${NFS_HOST}
        type: nfs
    service:
      app:
        ports:
          http:
            port: 3001
