---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: smtp-relay
spec:
  secretName: tls.smtp-relay
  issuerRef:
    name: letsencrypt-prod-clusterissuer
    kind: ClusterIssuer
  commonName: "smtp-relay.${ROOT_DOMAIN}"
  dnsNames: ["smtp-relay.${ROOT_DOMAIN}"]
