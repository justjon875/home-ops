apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: &app gitea
  namespace: utilities
spec:
  interval: 15m
  chart:
    spec:
      chart: *app
      version: 9.6.1
      sourceRef:
        kind: HelmRepository
        name: *app
        namespace: flux-system
      interval: 15m
  install:
    createNamespace: true
    remediation:
      retries: 5
  upgrade:
    remediation:
      retries: 5
  values:
    gitea:
      admin:
        existingSecret: gitea-admin-secrets
      config:
        cache:
          ADAPTER: redis
          HOST: "redis://:${REDIS_PASSWORD}@${REDIS_HOST}:6379"
        database:
          DB_TYPE: postgres
          HOST: ${POSTGRESQL_HOST}
        migrations:
          ALLOWED_DOMAINS: gitea.lithium.${ROOT_DOMAIN}
        queue:
          CONN_STR: "redis://:${REDIS_PASSWORD}@${REDIS_HOST}:6379"
        server:
          LANDING_PAGE: login
          ROOT_URL: https://gitea.${ROOT_DOMAIN}
          SSH_DOMAIN: &host gitea.${ROOT_DOMAIN}
        session:
          PROVIDER: redis
          PROVIDER_CONFIG: "redis://:${REDIS_PASSWORD}@${REDIS_HOST}:6379"
    image:
      repository: gitea/gitea
      # Overrides the image tag whose default is the chart appVersion.
      tag: "1.22.1"
      digest: "sha256:4468d6f4a314bed5ab273aae32d1214a344ca79ac641277cd35a9056d805e4ca"
    ingress:
      enabled: true
      className: internal
      annotations:
        nginx.ingress.kubernetes.io/proxy-body-size: "0"
      hosts:
        - host: *host
          paths:
            - path: /
              pathType: Prefix
      tls:
        - hosts:
            - *host
    memcached:
      enabled: false
    redis-cluster:
      enabled: false
    postgresql:
      enabled: false
    postgresql-ha:
      enabled: false
    persistence:
      create: false
      enabled: true
      claimName: gitea-pvc
    service:
      ssh:
        port: 2222
  valuesFrom:
    - kind: Secret
      name: gitea-config-database-secret
      targetPath: gitea.config.database.NAME
      valuesKey: NAME
    - kind: Secret
      name: gitea-config-database-secret
      targetPath: gitea.config.database.PASSWD
      valuesKey: PASSWD
    - kind: Secret
      name: gitea-config-database-secret
      targetPath: gitea.config.database.USER
      valuesKey: USER
