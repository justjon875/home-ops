apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app stump
  namespace: selfhosted
spec:
  chart:
    spec:
      chart: app-template
      interval: 15m
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      version: 3.4.0
  install:
    createNamespace: true
    remediation:
      retries: 5
  interval: 15m
  upgrade:
    remediation:
      retries: 5
  values:
    controllers:
      stump:
        containers:
          app:
            env:
              PGID: 1000
              PUID: 1000
              STUMP_PORT: 10801
            image:
              pullPolicy: IfNotPresent
              repository: aaronleopold/stump
              tag: nightly@sha256:7b0c82d2398e73de40b909f94e48a226d047dc3a29b7b415461dbf12eb02de67
    ingress:
      app:
        className: internal
        enabled: true
        hosts:
          - host: &host stump.${ROOT_DOMAIN}
            paths:
              - path: /
                pathType: Prefix
                service:
                  identifier: app
                  port: http
        tls:
          - hosts:
              - *host
    persistence:
      config:
        enabled: true
        path: ${NFS_PATH}/stump
        server: ${NFS_HOST}
        type: nfs
      data:
        enabled: true
        path: ${MEDIA_PATH}/books
        server: ${NFS_HOST}
        type: nfs
    service:
      app:
        controller: *app
        ports:
          http:
            port: 10801
