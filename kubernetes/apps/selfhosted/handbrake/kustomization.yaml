---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: selfhosted
resources:
  - ./helmrelease.yaml
  - ./volumes.yaml
labels:
  - pairs:
      app.kubernetes.io/name: handbrake
      app.kubernetes.io/instance: handbrake
