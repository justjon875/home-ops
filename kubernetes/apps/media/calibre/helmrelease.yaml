apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: calibre
  namespace: media
spec:
  chart:
    spec:
      chart: app-template
      interval: 15m
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      version: 3.1.0
  install:
    createNamespace: true
    remediation:
      retries: 5
  interval: 15m
  upgrade:
    remediation:
      retries: 5
  values:
    controllers:
      calibre:
        containers:
          app:
            env:
              PGID: 1000
              PUID: 1000
              TZ: ${TZ}
            image:
              repository: lscr.io/linuxserver/calibre
              tag: 7.10.0@sha256:fbd543c32b656bef73bf514765f053fe2c62b1e0d18b23cb773713fbf1249c55
    ingress:
      app:
        className: internal
        enabled: true
        hosts:
          - host: calibre.${ROOT_DOMAIN}
            paths:
              - path: /
                pathType: Prefix
                service:
                  identifier: app
                  port: http
        tls:
          - hosts:
              - calibre.${ROOT_DOMAIN}
      content-server:
        className: internal
        enabled: true
        hosts:
          - host: calibre-content.${ROOT_DOMAIN}
            paths:
              - path: /
                pathType: Prefix
                service:
                  identifier: content-server
                  port: http
        tls:
          - hosts:
              - calibre-content.${ROOT_DOMAIN}
    persistence:
      config:
        path: ${NFS_PATH}/calibre/config
        server: ${NFS_HOST}
        type: nfs
      media:
        globalMounts:
          - path: /data
        path: /mnt/raid/media/books
        server: ${NFS_HOST}
        type: nfs
    service:
      app:
        controller: calibre
        ports:
          http:
            port: 8080
      content-server:
        controller: calibre
        ports:
          http:
            port: 8081
